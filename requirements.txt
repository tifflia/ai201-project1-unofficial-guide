sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Document ingestion (Milestone 3)
#   Static pages (The Amherst Student):              requests + BeautifulSoup
#   JS-rendered pages (Reddit, amherst.edu, CollegeConfidential): Playwright
# After installing, run once to fetch the browser:  playwright install chromium
requests>=2.32.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
playwright>=1.40.0

# Query interface (Milestone 5)
gradio>=5.0,<6

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
